<% if @page %>
  <section class="about-section">
    <h2 class="page-title"><%= @page.title.capitalize %></h2> <!-- Changed to h2 for better accessibility -->
    <br>
    <div class="page-content-body"><%= raw @page.content %></div> <!-- raw allows HTML content to be rendered -->
  </section>
<% end %>